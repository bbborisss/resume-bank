# Application tracker

`/tailor-resume` appends a row here after each run. Update statuses yourself or just tell
Claude ("mark Northwind as interviewing"). Suggested statuses:
`applied` → `screen` → `interviewing` → `offer` / `rejected` / `ghosted` / `withdrawn`.

| Date | Company | Role | Folder | JD | Status |
|------|---------|------|--------|----|--------|
